
syntax = "proto3";

package hello;

// Here is an message that is shared between multiple services
// and can be imported by multiple proto files
message Person {    
    string name = 1;
    int32 age = 2;
}